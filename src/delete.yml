Virtual Machines virtual_machines: - virtual_machine_name: "weeu{{ subscription_shortname | lower } }{{ environment_shortname | first | lower } }{{ os_type | first | lower } }{{ application_shortname | lower } }01" virtual_machine_application_resource_group_name: "WeEu-{{ subscription_shortname | capitalize } }-{{ environment_shortname | capitalize } }-Rsg-{{ application_shortname | capitalize } }-{{ resource_group_increment } }" virtual_machine_os_type: "{{ os_type | capitalize } }" virtual_machine_size: 'Standard_DS3_v2'  virtual_machine_auto_shutdown_schedule: "{{ auto_shutdown_schedule } }" virtual_machine_component_name: "{{ component_name } }" virtual_machine_image_definition: "{{ image_definition } }" virtual_machine_image_version: "{{ image_version } }" virtual_machine_boot_diagnostics_primary_blob_endpoint: " https://weeu{{ subscription_shortname | lower } }{{ environment_shortname | lower } }sa{{ application_shortname | lower } }{{ resource_group_increment } }.blob.core.windows.net/" virtual_machine_commvault_backup: "{{ commvault_backup } }" - virtual_machine_name: "noeu{{ subscription_shortname | lower } }{{ environment_shortname | first | lower } }{{ os_type | first | lower } }{{ application_shortname | lower } }01" virtual_machine_application_resource_group_name: "NoEu-{{ subscription_shortname | capitalize } }-{{ environment_shortname | capitalize } }-Rsg-{{ application_shortname | capitalize } }-{{ resource_group_increment } }" virtual_machine_os_type: "{{ os_type | capitalize } }" virtual_machine_size: 'Standard_DS3_v2' virtual_machine_auto_shutdown_schedule: "{{ auto_shutdown_schedule } }" virtual_machine_component_name: "{{ component_name } }" virtual_machine_image_definition: "{{ image_definition } }" virtual_machine_image_version: "{{ image_version } }" virtual_machine_boot_diagnostics_primary_blob_endpoint: " https://noeu{{ subscription_shortname | lower } }{{ environment_shortname | lower } }sa{{ application_shortname | lower } }{{ resource_group_increment } }.blob.core.windows.net/" virtual_machine_commvault_backup: "{{ commvault_backup } }"